import React from 'react'

export const Toolbar = ({ ...props }) => {
  return (
    <nav>
      <button>Home</button>
      <button>New Book</button>
      <button>Add Document</button>
    </nav>
  )
}
